import { Box, UnstyledButton } from '@mantine/core'
import { FC } from 'react'

interface Props {}

export const Navbar: FC<Props> = () => {
  return (
    <Box component='nav'>
      <UnstyledButton>sdfsdf</UnstyledButton>
    </Box>
  )
}
