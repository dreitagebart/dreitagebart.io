import styled from "styled-components"
import { Box } from "@dreitagebart/box"

export const _Wrapper = styled(Box)`
  font-size: 60px;
  color: #efefef;
  margin: 40px;
  height: 100%;

  a {
    color: #afafaf;
  }

  a:hover {
    color: #efefef;
  }
`
